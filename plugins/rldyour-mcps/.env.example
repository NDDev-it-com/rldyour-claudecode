CONTEXT7_API_KEY=
GITHUB_PERSONAL_ACCESS_TOKEN=

# Optional Claude Code MCP runtime knobs (env-only — not part of .mcp.json schema):
# MCP_TIMEOUT=               # ms — server startup timeout
# MCP_TOOL_TIMEOUT=          # ms — per-tool call timeout
# MCP_CONNECTION_NONBLOCKING=  # 1 = do not block session start on slow MCP servers
