---
description: "Нормализовать репозиторий через ry-repair: source-of-truth scan → semantic entropy audit → repair plan → technical fixes → validators → docs/memory sync. Repair repository contracts and AI-tool context."
argument-hint: <scope-or-problem>
---

Ремонт и нормализация репозитория: **$ARGUMENTS**

Use the `ry-repair` skill for this request. The skill body is the single source of truth for source-of-truth discovery, deterministic install/update/sync checks through the root `ry_repair_sync.py` contract when present, semantic entropy detection, user-decision gates, technical-only repairs, validators, and sync closure.

Reply in Russian.
