---
description: "Синхронизировать завершенную задачу через flow-post-task-sync: Serena freshness → docs → checks → git/GitHub → fullrepo → cleanup. Finalize and synchronize task state."
argument-hint: <scope>
---

Финальная синхронизация для: **$ARGUMENTS**

Use the `flow-post-task-sync` skill for this request. The skill body owns Serena freshness, instruction-doc review, quality checks, atomic commits, upstream push, fullrepo publish, and safe branch/worktree cleanup.

Reply in Russian.
