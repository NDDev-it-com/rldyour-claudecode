---
description: "Запустить сквозной дизайн-воркфлоу через ry-design: Figma + tokens + FSD + shadcn/ui + ReactBits + browser validation. Run end-to-end design workflow."
argument-hint: <scope>
---

Сквозной дизайн-воркфлоу для: **$ARGUMENTS**

Use the `ry-design` skill for this request. The skill body orchestrates Figma context, design tokens, FSD layer placement, shadcn/ui primitives, ReactBits motion, and browser validation.

Reply in Russian.
