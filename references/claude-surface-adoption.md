# Claude Code Surface Adoption

Verified: 2026-07-03

Source of truth:
- Runtime baseline: `references/claude-baseline.json`
- Runtime package pin: `package.json`
- Official changelog: `https://code.claude.com/docs/en/changelog`

## Decisions

| Surface | Introduced | Decision | Implementation | Validator |
| --- | --- | --- | --- | --- |
| `disallowed-tools` skill/slash-command frontmatter | 2.1.152 | Adopt selectively | Use only for read-only/review/reporting routes where removing mutation tools reduces accidental capability. Keep positive `allowed-tools` routing as the default because it is easier to validate against plugin MCP namespaces. | `scripts/validate_skill_allowed_tools.py` |
| `/reload-skills` | 2.1.152 | Adopt operationally | Document as the operator command after plugin, skill, or marketplace updates. Do not call it from static validators. | `scripts/validate_claude_surface_adoption.py` |
| `SessionStart.reloadSkills` hook output | 2.1.152 | Future | Adopt only when a startup hook can cheaply prove that skill directories changed. Blind reload on every session start is unnecessary churn. | `scripts/validate_claude_surface_adoption.py` |
| `MessageDisplay` hook event | 2.1.152 | Intentionally unused | No current redaction or display-observability requirement justifies adding a high-volume hook. | `scripts/validate_claude_surface_adoption.py` |
| `/code-review --fix` and `/simplify` integration | 2.1.152 | Future | Keep rldyour reviewer tracks as the canonical review workflow until parity with file-first reviewer reports is designed and validated. | `scripts/validate_claude_surface_adoption.py` |
| `skipLfs` for `github`/`git` plugin marketplace sources | 2.1.153 | Future | This adapter does not currently use Git LFS-heavy plugin marketplace sources. Add only when marketplace source installs prove LFS download cost or failures. | `scripts/validate_claude_surface_adoption.py` |
| npm auto-update diagnostics and `/doctor` update reporting | 2.1.153 | Adopt operationally | Keep the package pin in `package.json`; use `/doctor` and `claude --version` in installed-runtime checks when local update failures are suspected. | `scripts/validate_release_state.py` |
| `settings.json` `statusLine` command and status line `COLUMNS`/`LINES` env | 2.1.153 | Adopted | `scripts/install-rldyour-marketplace.sh` stage 8 writes the managed `~/.claude/rldyour-statusline.sh` script and sets `settings.json` `statusLine` so every owner session shows model name, `context_window` used/left percentage, `rate_limits` five-hour/seven-day remaining percentage, and session cost. The script tolerates absent fields (rate limits appear only on subscription accounts after the first API response) and does not depend on `COLUMNS`/`LINES` terminal dimensions. | `scripts/validate_claude_surface_adoption.py` |
| `claude agents` native command and bundled skill autocomplete | 2.1.153 | Adopt implicitly | Existing agent names remain explicit. No config migration is required. | `scripts/validate_agent_tools.py` |
| Opus 4.8 via `opus` and `opus[1m]` aliases | 2.1.154 | Adopted | Keep `ry-explore` on `model: opus[1m]`; on Anthropic API the alias resolves to Opus 4.8, and Claude Code v2.1.154+ is the minimum runtime for that target. | `scripts/validate_claude_surface_adoption.py` |
| Dynamic workflows and `/workflows` | 2.1.154 | Hybrid pending native workflow | Keep `/rldyour-flow:ry-start` as the stable plugin command and route workflow-capable Claude Code sessions to the project `ry-start-workflow` boundary only when a saved `.claude/workflows/` artifact exists and installed-runtime smoke proves it. `plugins/rldyour-flow/skills/ry-start/SKILL.md` remains the fallback because official docs expose saved-workflow creation through the interactive `/workflows` view, not a stable file-format generator. | `scripts/validate_claude_surface_adoption.py`; root `scripts/smoke_claude_ry_start_workflow.py` |
| Streaming tool execution enabled by default | 2.1.154 | Adopt implicitly | No config migration is required; existing hooks and reviewer file-first output must continue to treat tool output as bounded evidence. | `scripts/validate_claude_surface_adoption.py` |
| Piped `claude mcp list`/`get` pending-approval reporting | 2.1.154 | Adopt operationally | Keep MCP approval checks in installed-runtime diagnostics; do not auto-approve `.mcp.json` from static validators. | `scripts/validate_claude_surface_adoption.py` |
| Plugin `defaultEnabled: false` metadata | 2.1.154 | Future | All first-party rldyour plugins remain enabled by the owner marketplace installer; add this only for optional plugins with explicit owner approval. | `scripts/validate_claude_surface_adoption.py` |
| Opus 4.8 thinking-block API hotfix | 2.1.156 | Adopted | Preserve the hotfix introduced in `2.1.156`; pin package/runtime surfaces to Claude Code `2.1.202` and require installed-runtime smoke to report `claude --version` `2.1.202` or newer before declaring Opus 4.8 release readiness. | `scripts/validate_claude_surface_adoption.py` |
| `.claude/skills` direct loading, `claude plugin init`, and `/plugin` autocomplete | 2.1.157 | Adopt operationally | Treat these as compatible with the existing marketplace model; keep first-party release artifacts in marketplace plugins and use `.claude/skills` only for generated/runtime discovery bridges where the control plane owns them. | `scripts/validate_claude_surface_adoption.py` |
| `settings.json` `agent` field honored | 2.1.157 | Adopt implicitly | Keep agent routing explicit in first-party command and skill metadata; no config migration is required because the existing settings surface can now be trusted when installed-runtime smoke exercises it. | `scripts/validate_agent_tools.py`; `scripts/validate_claude_surface_adoption.py` |
| `EnterWorktree` and background-agent worktree fixes | 2.1.157 | Adopt implicitly | Treat as runtime correctness for worktree-aware tasks; keep rldyour worktree cleanup and git synchronizationing policy in Flow rather than inventing adapter-specific worktree orchestration. | `scripts/validate_claude_surface_adoption.py` |
| `tool_decision` tool parameters with `OTEL_LOG_TOOL_DETAILS` | 2.1.157 | Future | Do not enable high-detail OpenTelemetry tool-parameter logging by default; use only for explicit diagnostics where logs are treated as sensitive evidence and never committed. | `scripts/validate_claude_surface_adoption.py` |
| ultracode keyword trigger setting | Current baseline | Future | Keep `/rldyour-flow:ry-start` and skill routing as the stable owner entrypoint until native saved workflows are created by Claude Code and installed-runtime smoke proves the ultracode trigger behavior. | `scripts/validate_claude_surface_adoption.py` |
| `CLAUDE_CODE_ENABLE_AUTO_MODE=1` provider Auto mode for Bedrock, Vertex, and Foundry | 2.1.158 | Hybrid | Document the environment variable for cloud-provider sessions using current provider-supported Opus Auto mode targets such as Opus 4.8, but keep the owner-local default launcher on `--dangerously-skip-permissions`; no local workflow may require provider Auto mode to be enabled. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.159 infrastructure-only release | 2.1.159 | Adopted | Official changelog records internal infrastructure improvements with no user-facing changes; update package/runtime pins and keep existing feature decisions unchanged. | `scripts/validate_runtime_baselines.py`; `scripts/validate_model_baselines.py` |
| Claude Code 2.1.161 secret-redaction and parallel-call isolation | 2.1.161 | Adopt implicitly | Runtime redacts sensitive `claude mcp` command output and isolates parallel tool-call failures. Repository repair scripts still redact captured command output before state logging instead of relying only on runtime behavior. | `scripts/validate_runtime_baselines.py`; `scripts/validate_model_baselines.py`; root `scripts/ry_repair_sync.py --plan --json` |
| Claude Code 2.1.162 agents/tooling and permission fixes | 2.1.162 | Adopted | Treat optional `waitingFor` in `claude agents --json` as runtime output data, allow native `Grep`/`Glob` tool listings on embedded-search builds, keep WebFetch permission rules explicit, and use installed-runtime smoke to prove the local binary. | `scripts/validate_runtime_baselines.py`; `scripts/validate_model_baselines.py`; root `scripts/smoke_claude_ry_start_workflow.py` |
| Managed runtime version bounds | 2.1.163 | Future | `requiredMinimumVersion` and `requiredMaximumVersion` are enterprise/admin managed settings. Do not write them into normal user or project config until this repository owns a managed settings surface; validate only when such a surface exists. | `scripts/validate_claude_surface_adoption.py` |
| `/plugin list` inventory command | 2.1.163 | Adopt operationally | Treat `/plugin list --enabled` and `/plugin list --disabled` as installed-runtime diagnostics. Static release truth remains marketplace and plugin manifests in this repository. | `scripts/validate_claude_surface_adoption.py` |
| Stop/SubagentStop `hookSpecificOutput.additionalContext` | 2.1.163 | Future | Keep Stop hook continuation owned by rldyour-flow until a specific hook needs to add feedback without surfacing as an error. Do not use this as a generic retry channel. | `scripts/validate_claude_surface_adoption.py` |
| Skills literal dollar escape before digits | 2.1.163 | Adopt operationally | When command bodies need a literal `$` before a digit, use the `\$` escape syntax and keep generated skill text ASCII-stable. No current command body migration is required. | `scripts/validate_claude_surface_adoption.py` |
| stdio MCP `CLAUDE_CODE_SESSION_ID` parity on resume | 2.1.163 | Adopt implicitly | Runtime now aligns resumed stdio MCP servers with hook/Bash session IDs; keep MCP config unchanged and rely on installed-runtime smoke when debugging resumed sessions. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.165 reliability rollup | 2.1.165 | Adopted | Official changelog records Bug fixes and reliability improvements only. Keep this as historical adopted runtime evidence; the current package/runtime baseline is `2.1.202`. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| `fallbackModel`, `--fallback-model`, deny rule glob, `SendMessage`, thinking disable, and managed MCP predicates | 2.1.166 | Adopt operationally | Document `fallbackModel` as owner/managed/user runtime policy without committing fallback model IDs; deny rule tool-name globs may include `"*"` while allow rules reject non-MCP globs; relayed `SendMessage` permission requests do not carry user authority; `MAX_THINKING_TOKENS=0` and `--thinking disabled` stay explicit operator overrides; future managed settings validation must cover `allowedMcpServers`, `deniedMcpServers`, and `${VAR}` matching. | `scripts/validate_claude_surface_adoption.py`; root `config/model-policy.json` |
| Claude Code 2.1.167 reliability rollup | 2.1.167 | Adopted | Official changelog records Bug fixes and reliability improvements only. Keep this as historical adopted runtime evidence; the current package/runtime baseline is `2.1.202`. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.168 reliability rollup | 2.1.168 | Adopted | Official changelog records Bug fixes and reliability improvements only. Keep this as historical adopted runtime evidence; the current package/runtime baseline is `2.1.202`. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.169 `--safe-mode`, `/cd`, `disableBundledSkills`, and `post-session` hook | 2.1.169 | Adopt operationally | The official changelog (published after the 2.1.169 pin) records `--safe-mode` / `CLAUDE_CODE_SAFE_MODE` (session with CLAUDE.md/plugins/skills/hooks/MCP disabled), the `/cd` command, the `disableBundledSkills` setting with `CLAUDE_CODE_DISABLE_BUNDLED_SKILLS`, and the self-hosted runner `post-session` lifecycle hook with a configurable SIGTERM-to-SIGKILL window. The owner standard remains the full-auto launcher: `--safe-mode` is a documented explicit diagnostic override, `/cd` is operator convenience, bundled skills stay enabled, and `post-session` applies only to self-hosted runner deployments this repository does not own. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.170 Claude Fable 5 rollup | 2.1.170 | Adopted | Official changelog introduces Claude Fable 5 (Mythos-class model) and fixes session transcripts not saving when launched from shells inheriting Claude Code env vars. No new adapter-owned config surface; keep as historical adopted runtime evidence. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.172 nested sub-agents and background agents hardening | 2.1.172 | Adopt operationally | Nested sub-agents are runtime capability only and do not relax rldyour worker policy: reviewer fanout and cmux workers still require explicit owner/workflow delegation. Marketplace plugin search, Bedrock region precedence, MCP/background agents fixes, and `availableModels` allowlist fixes are adopted as runtime correctness with no adapter config migration. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_runtime_baselines.py` |
| Claude Code 2.1.173 Fable 5 model names and Windows sandbox fixes | 2.1.173 | Adopted | Official changelog fixes Fable 5 model names with the 1M suffix and a spurious Windows sandbox startup warning. Pin package/runtime surfaces to Claude Code `2.1.202`; no new marketplace, hook, skill, or MCP config surface is required. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.175 managed model allowlist tightening | 2.1.175 | Adopted | Official GitHub release `v2.1.175` adds `enforceAvailableModels`; when enabled, the managed `availableModels` allowlist also constrains the Default model and user/project settings cannot widen the managed list. This adapter records the runtime requirement but does not create a managed settings surface unless owner policy adds one. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.176 language, footer, and policy fixes | 2.1.176 | Adopt operationally | Official GitHub release `v2.1.176` adds language-specific session titles, the `language` setting, `footerLinksRegexes`, Bedrock credential-cache expiry handling, stricter `availableModels` enforcement for `ANTHROPIC_DEFAULT_*_MODEL` and `/fast`, Fable 5 Auto-mode fallback, hook `if` path matching for Read/Edit/Write, and multiple Remote Control/background-session fixes. No committed settings surface changes are required; keep these as runtime correctness and documentation evidence. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.177 runtime rollup | 2.1.177 | Adopted | Official GitHub release `v2.1.177` was published with empty release notes; at that time npm `@anthropic-ai/claude-code` latest resolved to `2.1.177`. Treat as historical adopted runtime evidence; the current package/runtime baseline is `2.1.202` (see the 2.1.202 runtime rollup row). | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.178 agent-team and permission-parameter changes | 2.1.178 | Adopt operationally | Official changelog removes the `TeamCreate`/`TeamDelete` tools (with `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` every session has one implicit team and the Agent tool `team_name` parameter is accepted but ignored), adds `Tool(param:value)` permission-rule matching with a `*` wildcard (e.g. `Agent(model:opus)`), and refines nested `.claude/skills` and nested `.claude/` closest-wins resolution. This adapter keeps explicit reviewer/worker delegation through rldyour-flow rather than implicit agent teams; `Tool(param:value)` is available for owner permission policy but no managed permission surface is committed here. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.179 reliability rollup (npm stable) | 2.1.179 | Adopted | Official changelog records bug fixes and reliability improvements only (mid-stream connection drops now preserve partial responses; WSL2 mouse-wheel scroll fix). The npm `@anthropic-ai/claude-code` `stable` dist-tag resolves to `2.1.179`; the owner tracks npm `latest`. Keep as historical adopted runtime evidence. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.181 `/config key=value`, sandbox Apple Events, and Bun 1.4 | 2.1.181 | Adopt operationally | Official changelog adds `/config key=value` (interactive, `-p`, and Remote Control), the `sandbox.allowAppleEvents` opt-in, the `CLAUDE_CLIENT_PRESENCE_FILE` push-suppression variable, and upgrades the bundled Bun runtime to 1.4. `/config key=value` is operator convenience; `sandbox.allowAppleEvents` stays off because the owner launcher bypasses the sandbox via `--dangerously-skip-permissions`; no committed settings surface is required. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.183 Auto-mode destructive-command safety | 2.1.183 | Adopt operationally | Official changelog hardens Auto mode: destructive git (`git reset --hard`, `git checkout -- .`, `git clean -fd`, `git stash drop`) is blocked unless requested, `git commit --amend` is blocked when the commit was not made by the agent this session, and `terraform`/`pulumi`/`cdk destroy` are blocked unless the specific stack is named; it also adds deprecated-model warnings and the `attribution.sessionUrl` setting. The owner standard `--dangerously-skip-permissions` bypasses Auto-mode classification, so these guards apply to Auto-mode sessions rather than the owner bypass launcher, and the rldyour-flow Stop/sync workflow already owns destructive-git confirmation policy. Documented as runtime safety with no adapter config change. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.185 reliability rollup | 2.1.185 | Adopted | Official changelog records a single bug fix: the stream-stall hint now reads "Waiting for API response - will retry in ..." and triggers after 20s of silence instead of 10s. No adapter surface change; keep as historical adopted runtime evidence. | `scripts/validate_runtime_baselines.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.186 `claude mcp login/logout` and `respondToBashCommands` | 2.1.186 | Adopt operationally | Official changelog adds `claude mcp login <name>`/`claude mcp logout <name>` (with `--no-browser` for SSH), `/workflows` status filtering, a Skills section in the `/plugin` Installed tab, the `teammateMode: "iterm2"` setting, an AWS credential-refresh `/login` option, and makes `!` bash commands trigger an automatic model response unless `"respondToBashCommands": false` is set. CLI MCP login is adopted operationally for installed-runtime auth; the owner keeps `.mcp.json` approval in installed-runtime diagnostics, and no committed settings surface is required. | `scripts/validate_claude_surface_adoption.py` |
| Claude Code 2.1.187 `sandbox.credentials`, org model restrictions, and structured-output fix | 2.1.187 | Adopt operationally | Official changelog adds the `sandbox.credentials` setting (blocks sandboxed commands from reading credential files and secret environment variables), org-configured model restrictions across the model picker, `--model`, `/model`, and `ANTHROPIC_MODEL`, mouse-click support in fullscreen menus, the `CLAUDE_CODE_MCP_TOOL_IDLE_TIMEOUT` override for hung remote MCP tool calls, and fixes structured output so the model can no longer re-call `StructuredOutput` indefinitely after success (workflow `agent({schema})` now reliably returns structured output). `sandbox.credentials` aligns with the repo secret-handling posture but is documented as available hardening, not wired into owner config until a managed settings surface is owned (matching the `enforceAvailableModels` decision); the structured-output fix improves rldyour workflow reliability with no config change. | `scripts/validate_claude_surface_adoption.py`; `scripts/secret_scan.py` |
| Claude Code 2.1.195 runtime rollup | 2.1.195 | Adopted | Historical package/runtime baseline. The official changelog (`v2.1.195`, published 2026-06-24) records bug fixes and reliability improvements only; npm `@anthropic-ai/claude-code` `latest` resolved to `2.1.195` while `stable` resolved to `2.1.179` during that refresh. The 2.1.178-2.1.189 feature and reliability releases above were folded into this rollup. The current package/runtime baseline is `2.1.201` (see the 2.1.201 runtime rollup row). | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.196 runtime rollup | 2.1.196 | Adopted | Historical package/runtime baseline. The official GitHub release `v2.1.196` records runtime improvements for the `claude agents` completed-list display and Remote session startup provisioning checklist; npm `@anthropic-ai/claude-code` `latest` resolved to `2.1.196` at adoption time. Pin package/runtime surfaces to Claude Code `2.1.202`; installed-runtime smoke should report `claude --version` `2.1.202` or newer before declaring runtime-current readiness. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.197 Sonnet 5 default | 2.1.197 | Adopted | Official GitHub release `v2.1.197` introduces Claude Sonnet 5 as the default model in Claude Code, with a native 1M-token context window. The adapter records this as a runtime capability update; owner model policy still separately controls whether workflows target Opus 4.8 selectors or Sonnet defaults. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.199 runtime rollup | 2.1.199 | Adopted | Official GitHub release `v2.1.199` is a runtime reliability rollup. It keeps stacked slash-skill invocations loaded together and fixes several subagents, background-agent, hook stderr, retry, and streaming-error paths. Historical package/runtime baseline superseded by the 2.1.201 runtime rollup below. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.201 runtime rollup | 2.1.201 | Adopted | Official GitHub release `v2.1.201` is a runtime reliability rollup folding in the `2.1.200` release; npm `latest` resolved to `2.1.201` (published 2026-07-03) at adoption time. Historical package/runtime baseline superseded by the 2.1.202 runtime rollup below. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |
| Claude Code 2.1.202 runtime rollup | 2.1.202 | Adopted | Official GitHub release `v2.1.202` (published 2026-07-06) adds the `/config` "Dynamic workflow size" advisory setting and `workflow.run_id`/`workflow.name` OpenTelemetry attributes, reverts `/review <pr>` to a fast single-pass review (multi-agent review stays on `/code-review <level> <pr#>`), and fixes runtime paths (Ctrl+R history search crash, background-session rename, Remote Control command/file delivery, workflow script unicode-escape parsing, resume-by-name latency with many worktrees, duplicate skill re-invocation context, installer download retries). npm `@anthropic-ai/claude-code` `latest` resolves to `2.1.202`. This is the current package/runtime baseline pinned across `package.json`, `references/claude-baseline.json`, and `config/mcp-runtime-versions.env`; no new marketplace, hook, skill, or MCP config surface is required, and this release also lands the installer stage-9 managed model/env defaults (`model` `opus[1m]`, `ANTHROPIC_DEFAULT_OPUS_MODEL=claude-opus-4-8`, bash timeout and max-output-token env) as adapter policy. | `scripts/validate_claude_surface_adoption.py`; `scripts/validate_live_runtime_latest.py` |

## AskUserQuestion And Owner Full-Auto

The owner-standard Claude launcher remains `claude --dangerously-skip-permissions`.
That bypasses permission prompts by design. Claude Code Auto mode is a separate
native mode where a classifier model can reduce permission prompts, but it has
plan/account/model requirements and does not create a repository-controlled way
to answer semantic `AskUserQuestion` prompts on the owner's behalf.

For rldyour workflows, semantic ambiguity is handled by the main model or the
reviewer/research agents before mutation. Permission prompts are handled by the
owner-standard bypass launcher. Do not add a custom auto-answer hook that
fabricates owner decisions.

## Validation

`scripts/validate_claude_surface_adoption.py` requires this file to contain an
explicit decision for every runtime surface listed in
`references/claude-baseline.json`, every Claude Code `2.1.154` workflow/model
surface, and every pinned `2.1.202` runtime surface that affects this
adapter's plugin, skill, hook, or marketplace behavior.
