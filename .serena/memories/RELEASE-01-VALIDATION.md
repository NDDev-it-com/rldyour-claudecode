<!-- Memory Metadata
Last updated: 2026-05-21
Last commit: 3b0ad53 chore(release): 0.6.4
Scope: VERSION, CHANGELOG.md, README.md, .claude-plugin/marketplace.json, plugins/*/.claude-plugin/plugin.json, config/rldyour-contract.json, docs/contract-matrix.md, .github/workflows/validate.yml, .github/workflows/dependency-check.yml, .github/workflows/dependency-review.yml, .github/workflows/semgrep.yml, .github/workflows/actionlint.yml, .github/dependabot.yml, scripts/validate_marketplace.sh, scripts/validate_plugin_versions.py, scripts/validate_boundaries.py, scripts/validate_contract.py, scripts/generate_contract_matrix.py, scripts/_mcp_parse.py, scripts/smoke_serena_memory_taxonomy.sh, docs/release-process.md, docs/adr/0006-mcp-hook-ownership-boundaries.md, pyproject.toml
Area: RELEASE
-->

# RELEASE-01-VALIDATION

## Purpose

Versioning, changelog, validation, tagging, and release evidence contracts for this marketplace.

## Source Of Truth

- `VERSION`: marketplace/release boundary version.
- `CHANGELOG.md`: Keep-a-Changelog release notes.
- `.claude-plugin/marketplace.json`: marketplace plugin versions.
- `plugins/<plugin>/.claude-plugin/plugin.json`: per-plugin behavior/cache-refresh versions.
- `README.md`: owner-facing catalog, version summary, smoke/check commands.
- `docs/release-process.md`: release process reference.
- `scripts/validate_marketplace.sh`: full validation harness.
- `scripts/validate_plugin_versions.py`: marketplace/manifest version consistency.
- `scripts/smoke_serena_memory_taxonomy.sh`: focused Serena memory taxonomy regression smoke.
- `scripts/validate_reviewer_contracts.sh`: reviewer output transport contract drift detector; asserts 9 invariant types across 6 reviewer agents + `reviewer-protocol.md`. Wired into `scripts/validate_marketplace.sh` after the "Agent tools allowlist validation" step.
- `scripts/validate_boundaries.py`: structural boundary enforcer; reads `config/marketplace-policy.json` and enforces 6 invariants (mcp_owner, hook_owners, plugin.json name self-consistency, plugin_dependencies parity, `agent_only_path_globs` parity, `runtime_exclude_globs` parity). Wired into `scripts/validate_marketplace.sh` and `.github/workflows/validate.yml`.
- `config/rldyour-contract.json`: cross-tool business contract for domains, public flows, skills, agent roles, hook lifecycle, and CI baseline.
- `scripts/validate_contract.py`: proves the local Claude adapter against real files and requires Codex/OpenCode mappings to be explicit.
- `scripts/generate_contract_matrix.py`: generates `docs/contract-matrix.md` and supports `--check` for CI freshness.
- `scripts/_mcp_parse.py`: shared MCP reference parser; `split_mcp_ref(ref, plugins) -> (plugin, server, tool) | None`. Imported by `validate_skill_allowed_tools.py` and `validate_agent_tools.py`. Added at HEAD `924256c` (commit `cd13d0a`).
- `pyproject.toml`: Ruff + Pytest + Pyright configuration; `extraPaths = ["scripts"]` enables `from _mcp_parse import ...` resolution; `include = ["scripts", "plugins", "tests"]`. Updated at HEAD `924256c`.

## Current Behavior

- Current release boundary at HEAD: `VERSION` is `0.6.4` (verified at `VERSION` file, HEAD `3b0ad53`).
- Per-plugin versions (verified from `bash scripts/validate_marketplace.sh` at HEAD `3b0ad53`): all 9 plugins at `0.6.4`.
- **0.6.4 release prep** (`3b0ad53`): closes the cross-implementation audit P2 gap. Added `config/rldyour-contract.json`, `scripts/validate_contract.py`, `scripts/generate_contract_matrix.py`, generated `docs/contract-matrix.md`, and `tests/test_validate_contract.py`. Local/CI validation now gates the cross-tool contract and matrix freshness. `.github/workflows/dependency-review.yml` added as a required PR dependency review gate pinned to `actions/dependency-review-action` v4.9.0 by commit SHA. All 9 plugins, marketplace `VERSION`, `package.json`, and `pyproject.toml` bumped to `0.6.4`. Verification at HEAD: `bash scripts/validate_marketplace.sh`, full pytest 144/144, `ruff check scripts/ plugins/ tests/`, `pyright`, `actionlint`, `bash scripts/smoke_mcp_runtime.sh`, and `python3 scripts/release_manifest.py`.
- **0.6.2 release prep** (`75c26e8`): all 9 plugins and marketplace `VERSION` bumped to `0.6.2`; Claude Code floor moved to `2.1.145`; MCP pins moved to `serena-agent==1.5.1`, `chrome-devtools-mcp@1.0.1`, and `github-mcp-server` v1.0.5; `/rldyour-flow:ry-sync` added as a thin `flow-post-task-sync` command wrapper; `validate_command_skill_drift.py` now validates delegated skill wrappers with different command basenames; `validate_boundaries.py` now enforces fullrepo `agent_only_path_globs` and `runtime_exclude_globs` parity; `validate_release_state.py` now checks `package.json`, `pyproject.toml`, and release manifest marketplace version parity. Verification at HEAD: `bash scripts/validate_marketplace.sh`, `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest`, `python3 -m ruff check .`, `pyright .`, `git diff --check`.
- **0.6.0 release** (8 commits `cbe4595`..`332e0e7`): public-readiness wave. Changes: (1) `chore(marketplace): remove owner email from marketplace.json` (`cbe4595`); (2) SECURITY.md, CONTRIBUTING.md, CODE_OF_CONDUCT.md, `.github/ISSUE_TEMPLATE/` (bug_report.yml, feature_request.yml, config.yml), `.github/PULL_REQUEST_TEMPLATE.md` added (`ad833b9`); (3) `.github/workflows/codeql.yml` Python + Actions semantic analysis, free for public repos (`28b2f74`); (4) `.github/workflows/pytest.yml` + `validate.yml` syntax-checks matrix expanded to `[ubuntu-latest, macos-latest]` (`7457a21`); (5) `sys.executable` for internal Python subprocess in 3 plugin scripts (`c432f54`); (6) VERSION + 9 plugins bumped to `0.6.0` (`d89d274`); (7) reviewer-wave HIGH+MEDIUM closures: gitleaks weekly schedule fix, SECURITY.md emoji removal, CONTRIBUTING.md typo+path fix, CodeQL `security-and-quality` query pack, ADR-0008 amendment, README ADR count to 11 (`332e0e7`). Tags planned: `marketplace--v0.6.0` + 9 `<plugin>--v0.6.0`. Reviewer wave: `2026-05-18T...`. Verified at `VERSION` (`0.6.0`) and `python3 scripts/validate_plugin_versions.py` at HEAD `d89d274`.
- **0.6.1 patch follow-up** (4 commits `4f66127`, `38a0da4`, `a413172`, `b4e63ec`): closes remaining 3 reviewer follow-ups + CI egress hardening. Changes: (1) `docs/security/threat-model.md` CodeQL re-framing, `scripts/smoke_serena_memory_taxonomy.sh` + `scripts/validate_release_state.py` `sys.executable` migration (`4f66127`); (2) `validate.yml` harden-runner egress allowlist gains `mcp.deepwiki.com:443`, `mcp.grep.app:443`, `mcp.figma.com:443`, `developers.openai.com:443` (`38a0da4`); (3) `actionlint.yml` harden-runner egress gains `release-assets.githubusercontent.com:443` (`a413172`); (4) `docs/security/threat-model.md` + `docs/adr/0008` clarify that GitHub Secret Scanning + Push Protection are intentionally disabled - org enterprise policy blocks them, NOT planned for enablement (`b4e63ec`). VERSION not bumped (post-tag patch commits). Verified at files above at HEAD `b4e63ec`.
- **0.2.1 release** (`e3d146b`): `rldyour-flow` bumped to `0.2.1` for the D29 reviewer output transport contract (file-first output, `.serena/reviews/` runtime dir, run_id coordination, `RLDYOUR_REPORT_EOF` precursor pattern). Tags pushed: `marketplace--v0.2.1`, `rldyour-flow--v0.2.1`. All other 8 plugins remained at `0.2.0`.
- **0.2.2 release** (`0ff613d`): `rldyour-flow` bumped to `0.2.2` for wave-2 reviewer transport hardening after self-bootstrap review wave `2026-05-16T1433Z-e3d146b`. Changes: `RLDYOUR_REPORT_EOF` heredoc EOF marker replacing short tokens (`MD`, `EOF`) that could cause early termination; explicit Bash write boundary `<report_dir>/<reviewer-name>.md`; mandatory orchestrator `Read` of each `critical`/`high` report file before disposition; `run_id` canonicalized to `<UTC-ISO-compact>-<git-short-sha>`; `info` severity added to enum in `reviewer-protocol.md` line 24; `.serena/diagnostics/**` and `.serena/reviews/**` added to `RUNTIME_EXCLUDE_PATTERNS` in `fullrepo_sync.py` lines 50-53. Tags pushed: `marketplace--v0.2.2`, `rldyour-flow--v0.2.2`. Verified via `git tag --list "*--v0.2.2"` at HEAD `0ff613d`.
- **HEAD `61b913d` (no version bump)**: `scripts/validate_reviewer_contracts.sh` added (173 lines); wired into `scripts/validate_marketplace.sh`. This is a repo-level script addition only - no plugin runtime files changed, no `rldyour-flow` version bump, no new release tag. Wave-3 audit (`2026-05-16T1538Z-0ff613d`) F-3 verification info finding (confidence 85: "no automatic check for heredoc marker drift") closed via this script. All per-plugin versions remain at 0.2.0 / `rldyour-flow` at 0.2.2.
- **All 4 new tags in this wave** (verified via `git tag --list "*--v0.2.1" "*--v0.2.2"`): `rldyour-flow--v0.2.1`, `marketplace--v0.2.1` (backfilled at `e3d146b`), `rldyour-flow--v0.2.2`, `marketplace--v0.2.2` (at `0ff613d`).
- **HEAD `557bc00` (no version bump)**: 4 commits close 5 audit findings D31-D35 from review wave `2026-05-16T1859Z-61b913d`. Changes: loop guard fingerprint field parity in `flow_post_task_state.py` (D31, `23901c6`); compound `.sync_marker` in `stop_memory_sync.sh` (D32, `23901c6`); non-dict payload stderr surface in `serena_memory_state.py` (D33, `d563ea5`); CI steps `validate_reviewer_contracts.sh` + `smoke_mcp_runtime.sh` added to `.github/workflows/validate.yml` (D34, `dcbc7cc`); semgrep container digest-pinned in `.github/workflows/semgrep.yml` (D35, `dcbc7cc`); `CHANGELOG.md` `[Unreleased]` section updated (`557bc00`). No `plugins/*/plugin.json` version bumps - all changes are script body / hook script / CI workflow files. VERSION remains `0.2.2`.
- **HEAD `00d3f82` (no version bump)**: 3 doc-fix commits close wave-3 audit findings D36-D38. Changes: `validate_reviewer_contracts.sh` header corrected to list all 9 invariants with concrete PASS breakdown (`6f0c70d`); `rldyour-rules` cross-plugin path to `fullrepo_sync.py` canonicalized from `${CLAUDE_PLUGIN_ROOT}/../rldyour-flow/scripts/...` to `python3 "$(git rev-parse --show-toplevel)"/plugins/rldyour-flow/scripts/...` (`9a49121`, PATTERNS-01-CANONICAL F-2 closure); `config/REVIEW.md.template` added (68 lines, all 6+ sections, `00d3f82`, verification F-4 closure). No `plugins/*/plugin.json` version bumps. VERSION remains `0.2.2`.
- **0.2.3 release** (`d58f4ce` + `c041fac` + `5bd57ae`, wave-5/6/7, 2026-05-17): marketplace `VERSION` bumped to `0.2.3`. `rldyour-flow` bumped `0.2.2 -> 0.2.3`. All 8 other plugins bumped `0.2.0 -> 0.2.1` (em-dash normalization touched `plugin.json` description and skill bodies). 603 em-dash occurrences replaced codebase-wide. Security wave D39-D41 (`c041fac`): egress-policy block in all 4 CI workflows, fullrepo false-positive fix in `local_git_ai_guard.sh`, `RLDYOUR_FORCE_BOOTSTRAP=1` audit trail in `bootstrap_check.sh`. Security/verification wave D42-D46 (`5bd57ae`): INJECTION_MARKERS + BiDi BIDI_CONTROLS, `sanitize_for_advisory()` helper, `openai-docs` in READ_ONLY_BY_DESIGN_MCPS, `smoke_serena_memory_taxonomy.sh` D20+D21 completeness step, `validate_marketplace.sh` inline R5 rationale. Tag planned: `marketplace--v0.2.3`. Cache namespace: `~/.claude/plugins/cache/rldyour-claudecode/rldyour-flow/0.2.3/`; all other 8 plugins at `0.2.1/`. Verified via `cat VERSION` and `python3 scripts/validate_plugin_versions.py` at HEAD `5bd57ae`.
- **0.4.1 release** (`a81fe4e` fix, `eb295c0` bump): two validator gaps (CONS-1 + CONS-2) and three pytest failures from 0.4.0 closed. All 9 plugins and marketplace `VERSION` bumped to `0.4.1`. Tags pushed: `marketplace--v0.4.1` plus 9 plugin tags `<plugin>--v0.4.1`. CI: 12 workflows GREEN (10 release + pytest + gitleaks). Unit tests: 22 pass + 1 expected SKIP (was 3 broken at 0.4.0). See [[TECHDEBT-01-NOW]] D47 for the fix record. Verified via `cat VERSION` (`0.4.1`) and `python3 scripts/validate_plugin_versions.py` at HEAD `eb295c0`.
- **0.4.2 release** (`8b9a6c6` validators, `8a26e5a` CI, `de1ee41` docs, `a04c6eb` bump): four commits since 0.4.1. Changes: (1) SKIP messages in `scripts/validate_instruction_sync.py:83,87,94` and `scripts/validate_json_schemas.py:61` moved from stderr to stdout for stream consistency with sibling validators; (2) `scripts/validate_docs_canon.py:88` window heuristic changed from fixed `start - 30` to `max(30, len(knob) + 15)` to handle long knob names like `maxSkillDescriptionChars` (24 chars); `tests/test_validate_docs_canon.py` gained `test_long_knob_window_expands_dynamically` regression lock-in; (3) `.github/workflows/dependency-check.yml` egress allowlist gained `registry.npmjs.org:443`, `formulae.brew.sh:443`, `storage.googleapis.com:443` (was 7 endpoints, now 10 per lines 39-49); (4) `.github/workflows/validate.yml` CLI install step changed from hardcoded `'@anthropic-ai/claude-code@2.1.143'` to dynamic read from `package.json` via `steps.cli_version.outputs.version` (lines 55-65); (5) `plugins/rldyour-security/skills/ry-sec-review/SKILL.md:64-73` all 10 OWASP entries gained `:2025` year suffix, A09 wording changed from `and` to `&` to match canonical OWASP 2025 phrasing. All 9 plugins and marketplace `VERSION` bumped to `0.4.2`. Tags pushed: `marketplace--v0.4.2` plus 9 plugin tags `<plugin>--v0.4.2`. CI: 14 workflows GREEN. Unit tests: 23 pass + 1 expected SKIP (was 22 + 1 at 0.4.1). Verified via `cat VERSION` (`0.4.2`) and `python3 scripts/validate_plugin_versions.py` at HEAD `a04c6eb`.
- `CHANGELOG.md` `[0.2.2] - 2026-05-16` follows Keep-a-Changelog 1.1.0 with `### Changed` and `### Notes` subsections. `[Unreleased]` compare base updated to `marketplace--v0.2.2...HEAD`. `[0.2.2]` reference-link points to `https://github.com/NDDev-it-com/rldyour-claudecode/releases/tag/marketplace--v0.2.2`. Verified at `CHANGELOG.md` lines 742-743 at HEAD `0ff613d`.
- `CHANGELOG.md` `[0.2.1] - 2026-05-16` preserved: D29 reviewer output transport contract, file-first output, `.serena/reviews/` runtime artifact policy, `marketplace--v0.2.1` release boundary. Verified at HEAD `0ff613d`.
- `scripts/validate_marketplace.sh` is the broad local gate. CI workflows: `validate.yml` (SHA-pinned actions, harden-runner, `claude-code@2.1.143`, top-level `permissions: {}`), `dependency-check.yml` (same hardening), `semgrep.yml` (Semgrep OSS SAST), `actionlint.yml` (workflow YAML linter). Verified at HEAD `0ff613d`.

## Contracts And Data

- Plugin cache refresh depends on per-plugin `plugin.json` version strings. Content-only plugin changes under the same version can be missed by the runtime cache.
- **Post-change bump policy (project rule, owner-set 2026-05-17)**: after any change touching marketplace or plugin files - including documentation, memories, agent-only context, scripts, hooks, references, or skill bodies - bump BOTH the affected plugin versions AND the marketplace `VERSION`. This is stronger than the AGENTS.md cache-refresh rule (which limits bumps to `SKILL.md/agent.md/hooks.json/.mcp.json` runtime-cache-relevant changes). Rationale (owner): "после изменений версии всего маркетплейса и плагинов должны подниматься, чтобы все точно обновлялось". Concrete: every wave that modifies any tracked or agent-only file produces a new `VERSION` and at least one plugin version bump. Memories MUST also be updated and actualized in the same wave.
- If a plugin's runtime behavior changes, bump that plugin version and align the marketplace entry.
- If a change affects the overall release boundary, update `VERSION` and `CHANGELOG.md`.
- Tag convention for plugins: `<plugin-name>--v<version>` via `claude plugin tag --push`.
- Tag convention for marketplace boundary: `marketplace--v<version>` (introduced in 0.2.0; pushed manually alongside plugin tags). This is NOT a plugin tag - it marks the aggregate release boundary in the CHANGELOG reference-links block.
- Tags should be created only after the worktree is clean for tracked files and validation passes.
- Cache namespace: `~/.claude/plugins/cache/rldyour-claudecode/<plugin>/<version>/`; at HEAD `3b0ad53` all 9 plugins are at `0.6.3/`.
- **0.5.2 release** (10 commits `4dcc1d1` initial, `febf45f` canon, `10c8f06` defender, `01f6024` tests, `9a4c565` adr, `027b6f9` bump, `03afa2f` precise-semantics, `779350a` smoke-fix, `1be98e0` ci-tests, `da432c6` changelog): closes structural freshness-gate conflict for `main + fullrepo` branch-split projects (ADR-0011) plus defensive python3 resolver across all hooks. Changes: (1) `AGENT_INSTRUCTION_PATHS` extended from 14 to 23 paths mirroring full `.git/info/exclude` block (`febf45f`); (2) `_is_knowledge_path()` precise-match semantics - directory entries use `startswith`, exact files use `==`, `.aider` dotfile-family special case (F-3 closure, `03afa2f`); (3) inline `AGENT_INSTRUCTION_PATHS` mirror added to `mark_sync_required.sh` with drift enforced by `TestInlineHookCanonDrift` (`03afa2f`); (4) `tests/test_serena_memory_state.py` added (49 tests covering `AGENT_INSTRUCTION_PATHS` canon, negative classification, `.git/info/exclude` parity, inline hook mirror drift; `01f6024` + `1be98e0`); (5) defensive `PYTHON_BIN` resolver in all 9 hooks - 49 replacements (`10c8f06`); (6) `docs/adr/0011-agent-instruction-knowledge-equivalence.md` (MADR 4.0.0, status accepted, `9a4c565`); (7) `smoke_serena_memory_taxonomy.sh` rewritten for ADR-0011 invariant (F-1 critical closure, `779350a`); (8) `.github/workflows/pytest.yml` `paths:` trigger gained `plugins/**` (F-2 high closure, `1be98e0`); (9) `.codex/` aligned to `fullrepo_sync.AGENT_ONLY_PATTERNS` + `.git/info/exclude`; `.serena/project.local.yml` removed from `AGENT_INSTRUCTION_PATHS` (`03afa2f`). Reviewer wave: `2026-05-18T1238Z-027b6f9` (1 critical + 1 high + 4 medium surfaced; all closed in follow-up commits). Unit test count: 124 + 7 F-3 regression tests = 131 passing. Tags planned: `marketplace--v0.5.2` + 9 `<plugin>--v0.5.2`. Verified via `cat VERSION` (`0.5.2`) and `python3 scripts/validate_plugin_versions.py` at HEAD `027b6f9`.
- **0.5.1 release** (patch-level hardening wave, 11 commits `ac6a110` codeowners, `a3b9219` schema, `4d0f930` release, `4f429c1` docs, `cea0d73` hook, `c644334` conftest, `24d2290` hooks-perf, `22dc9d9` hook-ci, `d7d43c1` CI, `7effaa8` bump, `7f29df4` adr): closes 13 deferred findings from 0.5.0 reviewer wave. Changes: (1) Security F-4: CODEOWNERS expanded with explicit patterns for all security-critical files (`scripts/validate_*.py`, `scripts/smoke_*.sh`, `scripts/release_manifest.py`, `scripts/bootstrap_check.sh`, `scripts/_mcp_parse.py`, `scripts/check_mcp_runtime_versions.py`, `scripts/probe_mcp_upstream.py`, config policy files, plugin hook dirs, ADR dir; `ac6a110`). (2) Security F-8: `config/schemas/plugin.json` `dependencies[].version` field gained semver pattern (`^(?:[~^>=<]{0,2})?\\d+...` - blocks shell-injection chars; `a3b9219`). (3) Quality F-5 info + Integration F-3 + Security F-10: `scripts/release_manifest.py` `NPM_PIN_RE` first-char must be alphanumeric for both scoped and unscoped packages; WARN-to-stderr on null pin / missing `host_binary_pin` / missing `version_env` (`4d0f930`). (4) Security F-9 + Verification F-5 + Consistency F-3 partial: `docs/runtime-env.md` gained PAT scope guidance section, `RLDYOUR_SKIP_USER_PROMPT_HINT` in skip table, intentional-absence note for `RLDYOUR_SKIP_ENV_CHECK` (`4f429c1`). (5) Consistency F-3: `RLDYOUR_SKIP_USER_PROMPT_HINT=1` short-circuit added to `plugins/rldyour-serena-mcp/hooks/user_prompt_submit.sh` line 25 (`cea0d73`). (6) Consistency F-1: `tests/conftest.py` `patch_repo_root` copy list alphabetized at lines 165-183 (`c644334`). (7) Architecture F-2 + Security F-6: Serena `PreToolUse` and `PostToolUse` replaced 1 broad `Bash(git *)` handler each with 5 explicit handlers (`git commit*`, `git merge*`, `git cherry-pick*`, `git rebase*`, `git am*`); all handlers in both `hooks.json` files use `command: "/bin/bash"` (absolute path); hook script count now 9 (was 8) total; handler count 12 Serena + 7 flow = 19 (`24d2290`). (8) Security F-3: `plugins/rldyour-flow/hooks/pre_tool_use_ci_advisory.sh` added as new 9th hook script; 3 narrow handlers in flow `hooks.json` for `Bash(gh workflow*)`, `Bash(gh run*)`, `Bash(gh actions*)`; advisory only (exits 0); skip flag `RLDYOUR_SKIP_CI_ADVISORY=1` (`22dc9d9`). (9) Architecture F-3 + Consistency F-8: `cross-platform.yml` dropped `pull_request: types: [labeled]` trigger; schedule moved before `workflow_dispatch` per README ordering convention (`d7d43c1`). (10) Security F-1: `docs/adr/0010-macos-egress-trust-gap.md` added recording macOS egress trust gap as accepted platform limitation (`7f29df4`). Deferred intentionally NOT closed: Consistency F-5 (`$defs` style variance, no functional benefit) and Consistency F-2 (retro CHANGELOG file count). Unit tests: 75 passed + 3 deselected (unchanged from 0.5.0 - no new tests in this wave). Reviewer contracts: 52 PASS / 0 FAIL (unchanged). Tags planned: `marketplace--v0.5.1` + 9 `<plugin>--v0.5.1`. Verified via `cat VERSION` (`0.5.1`) and `python3 scripts/validate_plugin_versions.py` at HEAD `7effaa8`.
- **0.5.0 release** (commits `614bdcf` hooks, `d2392fd` release parser, `ae74430` commands, `bdcf3ab` tests, `7048dbc` schema, `083632f` agents, `6163c25` check-mcp, `2561b00` bootstrap, `2551997` validators, `00b4c92` CI, `b5e78d4` bump, `e956be5` smoke fix, `69d1105` validator fixes, `a6bde4d` reviewer fixes): 16 commits since `main` @ `6f07fe8`. Reviewer wave: 6 reports in `.serena/reviews/2026-05-17T1448Z-b5e78d4/`. External-audit findings P0/P1/P2/P3 closed. Changes: (1) P0 `fix(hooks)`: both `hooks.json` files rewritten - `if` filter moved inside inner hook handler (was at matcher-group level); exec-form `command: "bash"` + `args: ["${CLAUDE_PLUGIN_ROOT}/hooks/X.sh"]` per CC v2.1.139+; Serena uses `Bash(git *)` broad rule + script-level self-filter; flow uses `Bash(git commit*)`. `config/schemas/hooks.json` rewritten with `$defs` (`matcherGroup`, `hookHandler`); `if` is `hookHandler` property; full v2.1.142 field set added. (2) P0 `feat(schema)`: `$schema` format on all 5 schemas changed from `"uri"` to `"uri-reference"` (RFC 3986); `plugin.json` `dependencies` now accepts string OR `{name, version}` object; component paths accept string OR array. (3) P1 `fix(release)`: `release_manifest.py` gains `NPM_PIN_RE` for unscoped npm pins + `host_binary_pins` block from `config/mcp-runtime-versions.env` + `claude_code_min_version`. (4) P1 `feat(validators)`: `scripts/validate_command_skill_drift.py` (171 lines) enforces command = thin skill wrapper (<=800 chars, delegation phrase, no workflow headings, checklist <=3 items); 8 unit tests at `tests/test_validate_command_skill_drift.py` (233 lines); FAIL output to stderr. Wired into `scripts/validate_marketplace.sh`, `.github/workflows/validate.yml`, and `.github/workflows/cross-platform.yml`. (5) P1 `test(harness)`: 14 `tests/test_*.py` files gained `timeout=30` on `subprocess.run()`. (6) P1 `feat(bootstrap)`: `scripts/bootstrap_check.sh` new mandatory step FAILs on missing CONTEXT7_API_KEY or GITHUB_PERSONAL_ACCESS_TOKEN; new `docs/runtime-env.md` documents all required/optional env vars. (7) P2 `feat(check-mcp)`: `--strict` flag promotes missing host binaries from INFO to FAIL. (8) P2 `refactor(agents)`: `ry-explore` description 1660->407 chars, `flow-memory-sync` 1574->550 chars. (9) P2 `refactor(commands)`: 8 slash commands thinned to <=800 chars each, delegate via `Use the \`<name>\` skill`. (10) P2 `feat(validators)` write-scope: `validate_reviewer_contracts.sh` gains negative write-scope test; PASS count 52 (10 invariant types: 8 per agent × 6 + 4 protocol). `validate_instruction_docs.py --require-agent-docs` wired into `validate.yml`. (11) P2 `ci(workflows)`: `.github/workflows/pytest.yml` runs `pytest -m "not integration"`; new `.github/workflows/cross-platform.yml` (Ubuntu+macOS on schedule+dispatch); `.github/CODEOWNERS`; `.github/workflows/README.md`. (12) P3 `refactor(hook)`: `user_prompt_submit.sh` rewritten with strong/weak/action-verb routing and single python3 parser; ACTION regex gains `\b` boundary + verb conjugations (`a6bde4d`). (13) Quality F-1 HIGH: `smoke_hooks.sh` discovery fixed - walks both `command` AND `args` tokens (`e956be5`). (14) `validate_boundaries.py` normalizes `{name, version}` dep dicts; drift validator FAIL to stderr; `validate.yml` gains drift + instruction-docs mandatory steps (`69d1105`). All 9 plugins at 0.5.0. Tags planned: `marketplace--v0.5.0` + 9 `<plugin>--v0.5.0`. Unit tests: 75 passed + 3 deselected (was 70 + 3 at 0.4.4). Reviewer contracts: 52 PASS / 0 FAIL. Verified via `cat VERSION` (`0.5.0`), `python3 scripts/validate_plugin_versions.py`, `bash scripts/validate_reviewer_contracts.sh`, and `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/ -m "not integration"` at HEAD `a6bde4d`.
- **0.4.4 release** (`f02235a` text-hygiene, `a3a30db` CI, `e9d5424` cosmetic, `e03f7bd` tests, `6f07fe8` bump): five commits since 0.4.3. Changes: (1) `config/text-hygiene-allowlist.json` added - externalises em-dash / en-dash / BIDI exemption sets from compiled-in constants to a config file (closes LOW-10 / architecture F-4). `scripts/validate_text_hygiene.py` gained `load_allowlists(root)` function with malformed-JSON fallback; `ALLOWLIST_EM/EN/BIDI` constants renamed `_DEFAULT_ALLOWLIST_*` to signal fallback role. (2) Six new validator test files: `test_validate_json_schemas.py` (2 tests), `test_validate_plugin_versions.py` (5), `test_validate_instruction_docs.py` (6), `test_validate_skill_routing.py` (4, 1 `@pytest.mark.integration`), `test_check_mcp_runtime_versions.py` (5, 1 `@pytest.mark.integration`), `test_probe_mcp_upstream.py` (8, 1 `@pytest.mark.integration`). CHANGELOG reports 28 new test cases joining 42 existing = 70 total (F-TEST-05 closure). `tests/conftest.py` extended: `config/text-hygiene-allowlist.json` stub, hook stub at `plugins/rldyour-flow/hooks/post_tool_use_commit_advice.sh` with embedded U+202E via `chr(0x202E)`, `rldyour-flow` plugin.json + marketplace entry; `patch_repo_root` copy list gained 5 scripts (`validate_instruction_docs`, `validate_skill_routing`, `validate_boundaries`, `check_mcp_runtime_versions`, `probe_mcp_upstream`). (3) `tests/test_validate_text_hygiene.py` `test_bidi_allowlist_skips_documented_file` unskipped - no longer `@pytest.mark.skip` (closes INFO-4). (4) `scripts/validate_instruction_sync.py:45` `CONTRACT_BLOCK_RE` gained `\s*` before closing `-->`: `r"<!--\s*sync_contract:\s*\n(.*?)\n\s*-->"` tolerates indented HTML-comment style (closes INFO-3). (5) `.github/workflows/claude-cli-drift.yml:21-26` concurrency block aligned with `dependency-check.yml`: `group: ${{ github.workflow }}-${{ github.ref }}`, `cancel-in-progress: true` (closes LOW-9). (6) `docs/adr/0003-bilingual-skill-descriptions.md:72` count updated from "15 routing cases" to "19" (closes INFO-2). (7) `tests/test_validate_docs_canon.py:91` `monkeypatch` parameter gained `pytest.MonkeyPatch` type annotation (closes INFO-6). All 9 plugins and marketplace `VERSION` bumped to `0.4.4`. CHANGELOG `[0.4.4]` describes this as the marketplace's first "zero outstanding actionable findings" state (all findings from review wave `2026-05-17T0948Z-12a2bdc` closed, R1-R4 open risks unchanged). Verified via `cat VERSION` (`0.4.4`) and `python3 scripts/validate_plugin_versions.py` at HEAD `6f07fe8`.
- **0.4.3 release** (`cd13d0a` validators, `b820b8b` tests, `66dbdf6` boundaries, `72c5665` CI, `924256c` bump): five commits since 0.4.2. Changes: (1) `scripts/_mcp_parse.py` shared MCP reference parser added; `validate_skill_allowed_tools.py` and `validate_agent_tools.py` both import from it, eliminating the divergent `MCP_PATTERN_RE`/`parse_mcp_tool` implementations. `validate_agent_tools.py` now reports `unknown plugin X` instead of silent pass for unknown plugin refs. `validate_release_state.py` subprocess.run calls gained `timeout=30s` + `TimeoutExpired` handling. `validate_text_hygiene.py` self-skip changed to `path.resolve() == Path(__file__).resolve()`. `pyproject.toml` gained `extraPaths = ["scripts"]` for Pyright resolution and `include = ["tests"]`. (2) `tests/test_mcp_parse.py` (9 tests) and `tests/test_validate_agent_tools.py` (7 tests) added; total unit tests at HEAD `924256c` is 39 pass + 1 expected SKIP (was 23 + 1 at 0.4.2). `tests/conftest.py` extended with `context7` HTTP server stub and `validate_agent_tools.py` + `_mcp_parse.py` in `patch_repo_root` copy list. (3) `scripts/validate_boundaries.py` enforces 4 structural invariants from `config/marketplace-policy.json` (mcp_owner, hook_owners, plugin.json name self-consistency, dependency parity); closes ADR-0006 gap. Wired into `scripts/validate_marketplace.sh` and `.github/workflows/validate.yml`. (4) `.github/workflows/validate.yml` gained 2 advisory steps: ruff check and pyright type check (both `continue-on-error: true`); pip install expanded to `ruff>=0.7`, `pyright>=1.1`, `pyyaml>=6`, `pytest>=8`. (5) All 9 plugins and marketplace `VERSION` bumped to `0.4.3`. Tags pushed: `marketplace--v0.4.3` plus 9 plugin tags `<plugin>--v0.4.3`. CI: 14 workflows GREEN. CHANGELOG `[0.4.3]` cites 8 finding IDs: A-LOW-5/6/7, A-MED-2/3, Q-F-2, Q-LOW-4/6. Verified via `cat VERSION` (`0.4.3`) and `python3 scripts/validate_plugin_versions.py` at HEAD `924256c`.

## Change Rules

- For manifest or version changes, run `python3 scripts/validate_plugin_versions.py` and `bash scripts/validate_marketplace.sh`.
- For MCP runtime changes, also run `python3 scripts/check_mcp_runtime_versions.py` and relevant MCP smoke checks.
- For hook or memory-sync behavior changes, run shell syntax checks, analyzer/state script checks, `bash scripts/smoke_hooks.sh`, and `bash scripts/smoke_serena_memory_taxonomy.sh`.
- Avoid `scripts/smoke_fullrepo_sync.sh` after editing agent-only files unless prepared to restore/reapply them; it runs `--bootstrap-init` and can restore agent-only files from `origin/fullrepo`.

## Cross-References

- Open and closed debt record: [[TECHDEBT-01-NOW]] (D28 0.2.0 boundary, D24-D27 Wave 5 closures, D19 R5 closure, D29 reviewer output transport 0.2.1/0.2.2, D31-D35 wave-3 audit closures, D36-D38 doc fixes, D39-D46 wave-3 security/verification 0.2.3, D47 0.4.1 validator gap closures, D48 0.4.2 validator+CI, D49 0.4.3 shared parser+boundaries, D50 0.4.4 test coverage+allowlist+cosmetic, D51-D65 0.5.0 audit closures, D66-D76 0.5.1 hardening wave, D77-D80 0.5.2 ADR-0011+hook freshness wave).
- Validation harness patterns: [[PATTERNS-01-CANONICAL]] (commit message, memory file pattern, tag conventions).
- Plugin manifest and Claude Code version facts: [[CLAUDECODE-01-PLUGIN-CANON]].
- Marketplace architecture and plugin version map: [[CORE-02-MARKETPLACE]].
- Hook lifecycle and smoke gates: [[HOOKS-01-LIFECYCLE]].
- Post-task SDLC sync workflow: [[FLOW-01-SDLC]].
- Memory taxonomy and index: [[CORE-01-INDEX]].
- MCP transport version pins: [[MCP-01-TRANSPORT]].
- OWASP security precision (D22): [[SECURITY-01-OWASP]].

## Verification

- `python3 scripts/validate_plugin_versions.py`: version consistency.
- `bash scripts/validate_marketplace.sh`: full marketplace validation including Serena taxonomy smoke.
- `git diff --check`: whitespace sanity.
- `python3 plugins/rldyour-serena-mcp/scripts/analyze_sync_scope.py --from-ref HEAD --to-ref HEAD`: no-op analyzer sanity.
- `bash scripts/smoke_serena_memory_taxonomy.sh`: targeted memory taxonomy gate.
- `bash scripts/smoke_hooks.sh`: hook smoke.
- `bash scripts/smoke_mcp_runtime.sh`: MCP runtime smoke.
- `bash scripts/smoke_mcp_capabilities.sh`: MCP capability smoke; `figma` can be skipped when auth/session env is absent.
- `bash scripts/validate_reviewer_contracts.sh`: proves reviewer output transport contract (9 invariant types, 6 agents + protocol) is drift-free; wired into `bash scripts/validate_marketplace.sh`.
- `python3 scripts/validate_boundaries.py`: proves structural ownership invariants (mcp_owner, hook_owners, plugin.json name + dependencies); wired into `bash scripts/validate_marketplace.sh` and `.github/workflows/validate.yml`.
- `python3 scripts/validate_contract.py && python3 scripts/generate_contract_matrix.py --check`: proves cross-tool contract and generated matrix freshness; wired into `bash scripts/validate_marketplace.sh` and `.github/workflows/validate.yml`.
- `~/.local/bin/ruff check scripts/ plugins/`: advisory lint gate (0 errors at HEAD `924256c`); wired into `.github/workflows/validate.yml` `syntax-checks` with `continue-on-error: true`.
- `~/.local/bin/pyright`: advisory type check (0 errors, 0 warnings at HEAD `924256c`); wired into `.github/workflows/validate.yml` `syntax-checks` with `continue-on-error: true`.

- **0.6.4 release** (`3b0ad53`): marketplace `VERSION`, `package.json`, `pyproject.toml`, all 9 plugin manifests, and README inventory are synchronized to `0.6.4`. Tags pushed: `marketplace--v0.6.4` plus 9 plugin tags. Local validation passed with `bash scripts/validate_marketplace.sh`, release-state parity, contract validation, README inventory check, and hook/memory taxonomy smoke.
